
name: Release tagged build

on:
  push:
    tags: [ 'v*' ]

permissions:
  contents: write

jobs:
  release-tags:
    # This job has contents:write, so never run a mutable upstream branch here.
    uses: GTNewHorizons/GTNH-Actions-Workflows/.github/workflows/release-tags.yml@8d2e9d2fcfd6ea1c5b8f7c7e15359ad031444c7d
    secrets: inherit
